"""Place your pre-trained weights here:

- vxm_breast.h5          -> Pre-trained VoxelMorph breast registration model
- autoencoder.pth        -> Pre-trained VQ-GAN / KL-Autoencoder weights
- radimagenet_vgg16.pth  -> VGG-16 weights pre-trained on RadImageNet

Without these weights, the pipeline will use dummy/drop-in implementations
for the affected modules. Replace them with real weights to get full performance.
"""
